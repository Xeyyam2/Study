# Taste

- User communicates in Azerbaijani and expects assistant replies in Azerbaijani. Confidence: 0.9
- Answers choice/confirmation prompts with terse single selections (e.g. "b", "1", "ok") and issues terse directives to proceed (e.g. "basla kodlamaya" — start coding), expecting the assistant to continue autonomously without further confirmation (including ignoring optional offers like a visual browser preview). Confidence: 0.9
- When encountering a technical or audit finding they don't understand, asks for a plain-language explanation ("bu ne demeydi?", or a naive causal hypothesis such as "maybe the Vercel deploy is why the local build fails") and expects the assistant to first verify against the actual code/error (read/grep the real failure), then explain simply with concrete facts — what the file is, where it's imported/used, and whether/how external data or infra (Vercel, OneDrive, Supabase) is involved — rather than repeating jargon. Confidence: 0.65
- Prefers extending/reusing existing working code with additive changes over rewriting it from scratch, when the existing implementation already covers most requirements. Confidence: 0.6
- For new content entries (universities, blog posts), images must be downloaded and stored locally as WebP (max 1600px, quality 80-85%) with local paths in the seed files — never hotlink external images (e.g. Unsplash) for new additions, and never use the remote-image seed helper for them. Confidence: 0.95
- Downloaded images are permanent project assets: never delete or replace them without an explicit instruction, and they must not be gitignored; record every image source/license in a CREDITS.md file. Confidence: 0.9
- University listing images must be authentic: real campus photos of the actual institution, not generic stock placeholders — the user calls existing stock images "inappropriate" (uygunsuz) and expects them replaced with real photos sourced from a matching external reference (e.g. a site with the same university list), then hosted locally. Confidence: 0.8
- Prefers downloaded image assets to be organized centrally: a single mapping file (slug → local path) that the seed applies programmatically, so every university pulls its hero/gallery from one place (per the user's request to "keep them in one file and serve from there") instead of scattered inline URLs. Confidence: 0.7
- Prefers downloaded image assets to be organized centrally: a single mapping file (slug → local path) that the seed applies programmatically, so every university pulls its hero/gallery from one place (per the user's request to "keep them in one file and serve from there") instead of scattered inline URLs. Confidence: 0.7
- For content additions, expects technical SEO verification: unique slugs (no collisions with existing seed), sitemap auto-coverage of new entries, JSON-LD schema compatibility, and no new remote image patterns added to next.config. Confidence: 0.75
- Uses external reference sites (e.g. StudyLeo) as the source of truth for content/feature parity: expects the assistant to fetch and analyze the site's structure/data (including embedded JSON-LD) and replicate the full dataset — programs, prices (discounted + original), universities, and logos — into the project, not a sample subset; this parity also extends to UI/UX interaction behavior (e.g. how a carousel pages through cards), not just data. Confidence: 0.7
- For factual data accuracy (tuition fees, program details), prefers values sourced from official/primary sources — the universities' own websites ("onların öz saytlarında olan qiymət və doğru məlumatlar") — and kept current (e.g. the 2025-2026 academic year), rather than third-party aggregators or stale seed values; research agents should fetch official fee pages and cite the source URL per figure. Confidence: 0.8
- Uses the official university websites themselves as the authority for state-university tuition updates: when updating prices, fetches each institution's own fee page/PDF rather than relying on the reference site or aggregators. Confidence: 0.7
- When importing an external dataset, prefers the approach that feeds it into the existing DB/seed architecture (scraper → normalize → DB seed) over sidecar static JSON in the repo or runtime API proxying to the source — confirmed the recommended architecture-conformant option ("A") over the alternatives. Confidence: 0.6
- In program/university listings, displays the university logo to the left of the university name, matching the reference site's card/table layout — and expects restored/improved logo assets to be used on every card surface (the user asked to put the newly restored professional logos on the university menu cards: "universitet menusunda olan cardların üstündəki logolarda istifadə elə — indi yüklədiyin loqolar professional görünür"). Confidence: 0.75
- Server actions and public API endpoints must not leak internal details to the client: log the real error server-side (console.error with a tagged prefix) and return a generic user-facing message — e.g. upload/change-password leaking Supabase error.message, and /api/health exposing DB presence (db: true/false) as a fingerprint. Confidence: 0.7
- Prefers lean DB queries: listing queries select only the columns the consumer actually renders, dropping heavy JSONB blobs (e.g. unused i18n tagline/description) from the SELECT rather than selecting everything; type invariants are preserved by mapping omitted columns to empty objects. Confidence: 0.6
- Prefers a single shared DB pool (`getPool()` in src/lib/db.ts) over multiple per-module pools with different max sizes against the same DATABASE_URL, to avoid exhausting connection limits — and keeps a re-export shim (crm/db.ts) so existing imports don't break. Confidence: 0.55
- In carousels/sliders, every visible card must have its own unique image — the user complained that several blog cover images looked identical ("blog cartlarimizin ortuy sekilleri bezilerinde eynidi onlarin hamisini ferqli sekiller ele" — some of our blog cards' covers are the same, make them all different) and expects no two cards to share the same cover; the accepted fix is to curate the carousel to items that each have a distinct local cover and map each slug to its own cover file at the section level rather than showing posts whose DB cover images are shared/mismatched. Confidence: 0.8
- For carousels/sliders, each arrow click should move the track by exactly one full card — not a whole page and never leaving a partial card — the user explicitly asked for one-card-per-click movement on both the featured and blog carousels ("herekt eliyende bir card qeder tam hereket olsun" — when it moves, let it move exactly one card), superseding an earlier page-based paging approach. Confidence: 0.75
- Carousel infinite looping must be directional and seamless (StudyLeo-style): pressing left always slides content in from the left, pressing right from the right, with no visible jump/teleport between the first and last page — the user rejected a modulo-wrap implementation that "threw" the last page on a left click ("sola basanda aparir axirinciya atir... sola basanda sol terefden gelsin saga basanda sag terefden study leodaki kimi"), so the fix is a clone/tripled-track with an invisible re-center snap, not first↔last wrapping. Confidence: 0.85
- Prefers compact, fixed-width cards in carousels over large full-width grid cards — when the featured cards became full-width the user said they were "cox boyukdu" (too big) and asked to restore the previous small size (280/320px); also asked to shrink the blog carousel cards for the same reason ("blog karti boyuk gorunur, olcusunu kicilt"). Confidence: 0.85
- When a UI behavior is fixed/validated on one component (e.g. the blog carousel), expects the same fix applied to its sibling components (e.g. the featured universities carousel) unprompted — the user asked "apply the same thing to the carousel cards too" ("eyni seyi caruseldeki cardlarada tetqbiq eliyek"), so similar sections should behave identically rather than drift apart. Confidence: 0.6
- Carousel cards must fit a whole number per viewport — no partially visible card peeking at the right edge: the user called "3.5 cards fitting" a defect and asked to size cards so exactly 4 fit on desktop (width 360px→280px at the 1280px breakpoint, with inner typography/padding scaled down to match); later extended this to BOTH the featured and blog carousels with fluid card widths computed from the viewport (`(viewportWidth - (perPage-1)*gap)/perPage`) so a full set always fits and a half card never shows ("cardlar ekranda yarim gorunmesin" — let no card appear half on screen). The per-page count is tuned per viewport from what the user actually sees on their own laptop: on their notebook (~1280px) the university carousel showed 3 and the blog showed 2, and they asked for 5 per page on both on their laptop ("istirem notebook ile 5 dene gorunsun"), with each transition still a full card. The first 5-card attempt keyed breakpoints off viewport width ({min:1280}) and never fired because the carousels measure their own container (`el.clientWidth`, capped at ~1232px by max-width minus padding) — the user caught this after deploy with a terse "axi blog helede 2 gosterir" (blog still shows 2); the accepted fix rebases per-page breakpoints to the measured container width (1024px+ → 5, gap 24→16px), keeping the breakpoint math consistent with the element actually being measured. When the user reports a pushed responsive fix didn't change their machine's behavior, re-check the measurement source/breakpoints against the real rendered container rather than re-deploying the same change. Confidence: 0.85
- For "top universities" style sections, wants a ranking-based top-N list (ranking ascending) with a substantial number of items (at least 10 cards), rather than only a small curated "featured"-flagged subset. Confidence: 0.6
- For carousels/sliders, wants NO autoplay: cards should move only when the user clicks the arrows — the user explicitly asked to stop the automatic movement ("avtomtaik hereketi dayandiraq... ele hereket eletdirek,odan studyleoda kimi olacaq" — stop the auto-motion, only move on click, then it will be like StudyLeo). Confidence: 0.85
- Carousel prev/next arrows must be fully visible, never half-clipped by an `overflow-hidden` viewport/track — when edge placement (`-start-5`/`-end-5`) gets cut off, move the arrows above the cards (top-right of the section, hidden on mobile where controls drop below); the user explicitly complained the arrows were "yarı halda görünür" (half-visible) and asked to raise them so they appear complete. Confidence: 0.8
- University cards in the featured carousel should be image-dominant: a single large photo with name/city/rating overlaid on it, plus a separate "Apply Now" button below the image — matching the StudyLeo card structure the user pasted as the reference. Confidence: 0.7
- For university listing images, prefers photos fetched from each university's own official website (not generic stock and not merely a third-party reference), then stored locally — and expects them placed with SEO in mind. Confidence: 0.85
- When asked to replace images for a named set of items (e.g. "the 17 universities I listed"), expects ALL of them updated with official photos — a partial pass (e.g. 5 of 17, leaving the rest on old/third-party images) is treated as a miss and called out ("onların hamısını düzəlt" / fix all of them). Confidence: 0.8
- Distinguishes the hero section's background photo from the university listing/carousel images: a request to swap university photos for official ones scopes to the listing/carousel cards, not the hero background (which should keep its existing "students on campus" style photo). When a change bleeds out of scope the user pinpoints the exact element by pasting the rendered <img> srcset/URL and asks to restore the previous image ("evvelki sekli qoy"). Confidence: 0.6
- Specifies desired UI features by pasting the reference site's raw HTML markup (e.g. StudyHub's logo-marquee `<div>/<ul>` structure with its mask-image gradient and scroll animation, or StudyLeo's right-side mobile drawer markup) together with a terse directive ("study hubdaki kimi" — like StudyHub; "study leoda bu sekilde ... o sekilde hell ele" — solve it the way StudyLeo does), expecting that exact structure and behavior to be replicated on the site. Confidence: 0.7
- Prefers slower, calmer marquee/logo-scroll motion — when the logo marquee ran at 40s the user said it was too fast ("logoların sürəti çoxu") and asked to slow it down (halved to 80s). Confidence: 0.55
- Curates UI content (e.g. which university logos appear in a marquee) iteratively and by eye: expects to visually review the result, then name the specific items to remove ("sənə hansı logoların görünmədiyini diyəcəm, onları çıxararsan" — I'll tell you which logos don't show, you remove them), so the implementation should make the item list easy to filter/remove by name/slug rather than hardcoding a one-shot set. Confidence: 0.5
- When site image assets break or are missing (e.g. university logos that fail the Next image optimizer, or a logo never downloaded for a university), expects them repaired first — re-downloaded/re-encoded from the original source (the StudyLeo S3 catalog's logoUrl or the university's own official site, e.g. BTU logo sourced from btu.edu.tr and added to the central logo map) — and only removed from the UI as a last resort ("görünməyən loqoları bərpa elə, eləmək olmursa onları çıxararsan" — restore the invisible logos; if you can't, then remove them). Confidence: 0.8
- When a next/image source fails the optimizer (e.g. SVG logos 400 on /_next/image), prefers serving that format unoptimized (a small `isSvgUrl` helper driving `unoptimized={...}` on every logo Image) over enabling `dangerouslyAllowSVG` in next.config — the latter is avoided as an unnecessary security surface; local SVG logos are then served directly with `image/svg+xml` and work in every listing/card. Confidence: 0.75
- When a fix for broken site assets (e.g. corrupt university logos) is started, verifying the repair on a single sample file does not count as solving the problem — the user expects the actual repair applied to every affected file, and follows up ("niyə həll etmədin?" — why didn't you fix it?) when broken items still render on the live site. Confidence: 0.8
- Marquee/logo-scroll animation must be seamless and gap-free: all items flow uniformly in one continuous loop with consistent spacing — the user complained logos appeared in groups of ~5 followed by a gap and asked that they all follow one another normally ("ele olsunki hamisi normal qaydada ard arda gelsinler" — make them all come consecutively in order). Confidence: 0.6
- Reports broken UI/bugs by pasting the exact error output from the live site (e.g. the list of Next.js image optimizer "received null" errors for each failing asset), expecting the assistant to root-cause from the real failure rather than guess. Confidence: 0.5
- Judges UI sections by visual modernity: when a section's visuals read as dated to the user ("sanki köhnə sayt sistemi kimi görünür" — looks like an old website system), expects a full premium redesign executed as a senior frontend/UI designer — gradient icon tiles with distinct per-card colors, hover lift/glow micro-interactions, ghost/depth numbers, pill price badges, ambient background glows, and a re-balanced grid — rather than small cosmetic tweaks; the user explicitly asks for "daha müasir və premium görünsün" (more modern and premium) and delegates the design decisions to the assistant without wanting confirmation questions. Confidence: 0.7
- Identifies the component to change by pasting the section's rendered content/data from the live page (e.g. the whole category list with program counts and "bundan X US$" prices) rather than naming the file or component — the assistant is expected to locate the matching section component (e.g. CategorySection) from that pasted text; the user repeats this pattern after each redesign (pastes the section again with the new ghost numbers to request the next tweak). Confidence: 0.6
- In card/grid sections (e.g. the category cards), expects every card to display the same complete set of elements — when some cards lack data (e.g. no price shown), add realistic fallback values so all cards look identical rather than leaving a subset bare; the user asked "bezilerinde qiymet yoxdu onlarda qiymet elave ele cardlar eyni gorunsun" (some have no price, add prices, make the cards look the same). Confidence: 0.8
- Prefers centered card layout in grids: icon centered at the top with centered text/price below (user asked "iconlari merkezlesdir" — center the icons) rather than a left-aligned icon row with a corner ghost number. Confidence: 0.6
- Expects UI label/copy changes to be applied literally and minimally as specified: when the user says a button's text should be "login" ("o duymenin ustundeki yazini deyisib login ele"), the label must be exactly the short word (translated per locale), not a longer paraphrase such as "Log in with Google" — the user corrected the assistant for embellishing the requested label, and repeated that the change must be translated in all languages. Confidence: 0.7
- Actively tests the mobile view and reports mobile-specific UI defects (e.g. the hamburger menu showing only Blog and Contact while other items are missing — "yalniz blog ve contact gorunur... hamisini gorunsun"): expects the mobile navigation to expose the full set of links, so fixes must make every item visible rather than leaving a subset; the assistant is expected to root-cause (menu code, nav translation keys across all locales, rendered HTML) rather than guess. Confidence: 0.6
- Fixed-position overlays/drawers (mobile menu, modals) must render above every other element and cover the full screen: when a `fixed` drawer was trapped in the sticky header's stacking context and hid behind the hero section (only ~5px visible), the accepted fix was rendering it via a React portal to `document.body` with a high z-index (`z-[100]`) rather than merely bumping z-index — the user explicitly asked "tam ekran boyunca goster onu" (show it full screen), so overlays must never be occluded by page sections. Confidence: 0.75
  site (e.g. the list of Next.js image optimizer "received null" errors for each failing asset), expecting the assistant to root-cause from the real failure rather than guess. Confidence: 0.5
  age optimizer "received null" errors for each failing asset), expecting the assistant to root-cause from the real failure rather than guess. Confidence: 0.5
