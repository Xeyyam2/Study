# Taste

## Workflow

- Strongly prefers strictly sequential execution: finish one unit (file, language, task) completely before starting the next; explicitly forbids parallel batch processing. Confidence: 0.95
- Wants a short summary after each completed unit of work and a final summary table at the end of a multi-step task. Confidence: 0.8
- Prefers the assistant to make cultural/contextual judgment calls autonomously rather than interrupting with questions. Confidence: 0.9
- Continues through the remaining steps of a multi-step task autonomously (e.g., next language/file) without waiting for an explicit "continue" command, unless the user explicitly stops. Explicitly instructs finishing the entire remaining backlog back-to-back with no per-item confirmation — "bitende sorusmadan digerine kec. hamisini bitir" (when done, move to the next without asking; finish everything). Confidence: 0.95
- Uses todo lists to track multi-step tasks and marks progress per step. Confidence: 0.7
- Before starting a large multi-part task, wants a brief scope confirmation first (how many items, which cities/topics, source choices), then autonomous execution. Confidence: 0.8
- Expects the brainstorming skill to be used to produce a plan/spec for content or design expansion tasks before implementation; explicitly asks for it ("brainstormingden istifadə edərək" / "brainstroming den istifade eliyerem") and expects brainstorming to precede coding. Confidence: 0.95
- Has a planning/audit-driven workflow: maintains a phased fix list (duzelis.md) whose completed phases may be marked done while their work is still uncommitted in the working tree; when executing a later phase, verify diffs of touched files before committing so previously completed-but-uncommitted work gets included in the commit. Confidence: 0.8
- Project shell environment is Windows (cmd/PowerShell): bash heredocs fail for multi-line commit messages, but PowerShell accepts a multi-line `git commit -m "...\n\n..."` string directly (observed working); writing the message to a temp file and committing with `git commit -F <file>` is the fallback when quoting gets messy. Confidence: 0.7
- The repo lives inside OneDrive on Windows (`C:\Users\Asus\OneDrive\Desktop\study`), and OneDrive sync repeatedly corrupts Next.js's `.next` cache — symptom is `EINVAL readlink` build errors or `next start` serving missing/corrupted routes (e.g. 404 on pages that exist under `.next/server/app`, empty `<main>` with an error boundary). Before blaming code for a weird local build/e2e failure, delete `.next` and rebuild clean; also check which process actually listens on the port: a stale server from an earlier session can keep port 3000 and silently serve old code while the freshly started dev server falls back to port 3001 — verify the listener with netstat and hit the right port before trusting a test result. Confidence: 0.85
- Never run destructive git commands (`git checkout -- <path>`, `git clean`, `git reset --hard`) on paths that may hold uncommitted work — always verify with `git status`/`git diff` first; uncommitted user work in this repo is common and is assumed precious. Also verifies provenance before deleting stray files (e.g. reads a script to confirm it was the assistant's own temp artifact vs. user work) rather than assuming they are disposable. Confidence: 0.65
- For repetitive multi-file edits (e.g., adding one key to many JSON/i18n files), prefers a small Node script with line-based insertion that preserves the original file formatting — a JSON parse/modify/round-trip rewrite is unacceptable because it reformats entire files and creates huge diffs. Confidence: 0.7
- Maintains the project plan/fix list in a markdown file (e.g. duzeli.md) with numbered phases and checkbox status markers ([✓]/✅ done, [•] in progress, [ ] pending); expects the assistant to read that file and execute the remaining unchecked/in-progress items. Confidence: 0.85
- Also tracks audit report findings (e.g. sehv.md) as a checklist with status markers (⬜ Açıq / ✅) and follows up on specific items by ID (e.g. "B7"), treating the audit report as a living fix checklist rather than a one-off document; proactively flips completed items to ✅ (with the fix described in the status column) and commits the doc together with the code fixes. Confidence: 0.7
- Expects the assistant to apply senior-level expertise across disciplines — frontend, UI, QA, backend, fullstack, SEO, DevOps, security ("senior hacker"), and performance ("senior suret/suret analizi") — both when executing multi-phase fixes ("as a senior SEO" (senior seo olaraq duzelt)) and when commissioning full audits ("saytimizi senior backend, senior frontend, senior ui, senior fullstack, senior hacker, senior seo, senior qa, senior devops, senior suretlendirme analizi uzre analiz ele"). Confidence: 0.95
- Explicitly rejects quick/shortcut fixes when commissioning work: "en tez ve en qisaa versiyasi ile duzeltmeye calisma... professional sekilde duzelt" (don't try to fix it with the fastest and shortest version — fix it professionally), expecting a complete production-grade solution (dedicated page/component, grouped results, empty states, i18n across all locales, full test/build verification) rather than a minimal patch; repeats the demand as "telesmeden hell ele professional sekilde olsun" (don't rush, solve it professionally). Confidence: 0.8
- Prefers pragmatic, YAGNI-driven decisions over large speculative refactors: when an audit offers a "remove misleading config" vs "full rewrite" choice, pick the minimal correct fix; skip features that would need external service credentials (Sentry DSN, Upstash Redis) rather than blocking, and defer them with a clear note. Confidence: 0.8
- When exhaustive static pre-rendering (`generateStaticParams` on heavy pages) makes the production build slow or timeout-prone, prefers fast, reliable builds with on-demand/ISR rendering instead — chose the "on-demand" option over keeping slow full static generation when the two conflicted. Confidence: 0.6
- When a review/test exposes dead code (e.g. an option field never populated by the data layer, a misleading test name, a failing stale test), removes or corrects it rather than preserving it for compatibility. Confidence: 0.8
- When a build failure is caused by a service that is simply down (e.g. Postgres unreachable — ECONNREFUSED), the accepted fix is to bring the infrastructure up (start the Docker container, run migrations, verify seed data) and rebuild, not to paper over the failure with code-level workarounds. Confidence: 0.6
- Accepts the assistant acting as an ops/DevOps engineer on their machine: starting Docker Desktop via PowerShell, starting/stopping containers, running DB migrations, and inspecting container/table state to unblock a build. Confidence: 0.5
- Commissioned site audits are analysis-only by design: the user asks to start with the analysis and report what is wrong, deliberately deferring fixes to a later session or another AI — deliver the audit report without implementing fixes mid-analysis, then offer next steps (writing the report to a file, starting fixes) for confirmation. Confidence: 0.65
- Audit reports must be concrete and independently verifiable: findings delivered as a table keyed by stable IDs (P1, S1, SEC1, B1, F1, Q1, D1…), each with a file:line reference, severity, and a specific fix, plus a prioritized shortlist of top actions (e.g. top-5 for speed, top-5 for Google #1) — so another AI can re-check the findings in a separate session. Confidence: 0.7
  ne: starting Docker Desktop via PowerShell, starting/stopping containers, running DB migrations, and inspecting container/table state to unblock a build. Confidence: 0.5
- Expects completed work to be committed AND pushed to the remote (GitHub) as part of finishing a task — explicitly instructs "və sonda push edirsən" (and at the end you push) and "sonda ugurlu olarsa push eliyersen" (if successful, push at the end), follows up with "push eledin?" / "push elemisen?" (did you push?) when work was left uncommitted/unpushed, and expects a confirmation that local and origin/main are in sync; when the user knows unrelated changes are still pending ("deyisiklik var onlarida push ele" — there are changes, push them too), expects those too to be found, committed and pushed rather than left behind in the working tree. Confidence: 0.95
- When the user explicitly narrows scope ("sadece X et, başqa heç nəyi dəyişmə" — just do X, don't change anything else yet), respect that boundary: make only the requested change and defer adjacent/related work (even work the user mentioned earlier, e.g. a Dormitory badge rename) for explicit confirmation instead of folding it into the same pass. Confidence: 0.7
- When a reported UI bug (e.g. mobile hamburger menu missing items) cannot be confirmed from static code and translations alone, prefers verifying against the real rendered page (starting a production server on a spare port and fetching HTML) rather than blindly rewriting the component; when the underlying cause still isn't reproducible, resorts to a deterministic rewrite that guarantees the desired behavior (explicit links instead of a data-driven map). Confidence: 0.5
- Mobile navigation/overlay fixes iterate through the same UI until it matches the reference site (StudyLeo): after explicit links and then a full-screen overlay ("tam ekran overlay"), the user pasted StudyLeo's drawer HTML and rejected the full-screen version ("bizde bele deyil") — the hamburger menu must now open as a right-side full-height drawer with a dark overlay behind it (clicking the overlay closes it), body scroll locked while open, and all menu items comfortably visible ("hamburger duymesine basanda sag terefde tam ekran boyunca acilsin"); this right-side-drawer style supersedes the earlier full-screen overlay, so new menu/panel reworks should follow the reference site's latest pattern. Confidence: 0.8
- When pushing to GitHub, expects selective commit curation — stage only files that belong in the repo (per .gitignore and existing tracked-file conventions), excluding local one-off artifacts such as scraped JSON/PDF data files, one-off DB scripts (insert-_/pgcheck-_), and redundant source images (e.g. .jpg when the optimized .webp is used); the user explicitly directs this ("push ele. githuba gedenleri push ele" — push only the ones that go to GitHub). Confidence: 0.7
  h as scraped JSON/PDF data files, one-off DB scripts (insert-_/pgcheck-_), and redundant source images (e.g. .jpg when the optimized .webp is used); the user explicitly directs this ("push ele. githuba gedenleri push ele" — push only the ones that go to GitHub). Confidence: 0.7
- Feature work is driven by design spec documents under `docs/superpowers/specs/` (e.g. 2026-08-17-header-search-design.md): the user says "burda yazilanlari tetbiq ele" (apply what's written here) and expects the spec implemented faithfully. When another AI has already completed part of a task, the user pastes its numbered progress notes and says "yerde qalan hisseni davam eletdir" (continue the remaining part) — expected behavior is to read the spec and the existing files, verify what is actually done vs. remaining (confirm i18n keys exist in all 18 locales, component placement, test state) rather than trusting the notes blindly, then finish the rest, run tests/typecheck/lint/build, and commit + push. Confidence: 0.75
- Hook unit tests must not rely on `@testing-library/react`'s `renderHook`/`act`: this repo's React 19.2 removed `act` from both `react` and `react-dom/test-utils` (vi.mock, aliasing, and monkey-patching all fail to restore it), so the established pattern is a custom `mountHook` harness using `createRoot` + `flushSync` (still exported by react-dom) with a Probe component and a `current` getter; debounce/async behavior is tested with real timers and short waits (~250ms past the 150ms debounce) rather than fake timers, and hook tests live under `tests/unit/components/` (jsdom via environmentMatchGlobs). Confidence: 0.65
- Site search should provide instant, live suggestions as the user types: typing a fragment (e.g. "ank") must immediately list matching universities/cities below the input to pick from, not only return results after pressing the search button — the user explicitly asked for this on the dedicated /search page ("men istiremki meselen ank yazan kimi asagida mene ankarada universitetlerin adlarini ... neticeler gostersin" — when I type "ank", show me the names of Ankara universities right below). Confidence: 0.65
