# Taste

See [taste/taste.md](taste/taste.md)

- Expects the project's production build (`npm run build`) to complete cleanly with no errors or warnings — pastes full build logs (prerender failures, `generateStaticParams` DB errors, missing-`metadataBase` warnings) and asks to fix them all ("bu xetalari aradan qaldir"); when the build fails because Postgres is unreachable (ECONNREFUSED), the expected fix is at the environment level (start the Docker Postgres container, run migrations, verify seed data) and rebuild, not masking the failure. Confidence: 0.55
- Codebase log/console messages are written in Azerbaijani (e.g. "DB xətası, [] qaytarılır") — new log strings in this project should match that language. Confidence: 0.4
- Site speed and #1 Google ranking are explicit top priorities: the user asks how to make the site "en suretli" (fastest) and "google'da en birinci" (first on Google) and reiterates wanting the site to always rank above all others ("saytımız bütün saytların ən üstündə gəlsin googlda hər zaman"), so audits and changes should be evaluated against performance and SEO impact. Confidence: 0.8
- The site is deployed to Vercel in production and the user self-manages deploys (they deploy via Vercel themselves; Vercel builds in its own cloud and never affects local builds). Local `.env.local` holds the real production Supabase connection (gitignored) — so local tooling must be kept off the live DB: DB-backed unit tests are redirected to the local Docker Postgres (localhost:5433) whenever the env points at supabase.co, and builds/e2e are run with `DATABASE_URL` explicitly overridden to the local database. Confidence: 0.6
